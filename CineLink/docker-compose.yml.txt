version: '3.8'

services:
  # 【名称修改】容器和服务名称
  cinelink:
    build: .
    container_name: cinelink
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - ./tmdb_system.db:/app/tmdb_system.db
      - /mnt/user/media:/data 
    environment:
      - TZ=Asia/Shanghai